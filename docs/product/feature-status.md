# Living Nutrition Feature Status

Last updated: 2026-07-22

This project is intentionally being built in phases. Camera-assisted macro tracking is a high-risk product area because a photo cannot reliably reveal exact weight, cooking oil, sauces, hidden ingredients, or edible portion. The app should treat vision as assisted entry, not final nutrition authority.

## Current State

For the canonical implementation snapshot, see [[Current State]]. For risks, see [[Known Issues]]. For planned phases, see [[Roadmap]].

### Implemented

- Monorepo with `apps/mobile`, `apps/api`, and shared packages.
- Expo SDK 54 mobile app with TypeScript strict mode and Expo Router.
- First-run onboarding with value, accuracy-control, selectable goal framing, optional reviewable daily-target setup, optional dietary-preference selection, logging-method choice, and optional camera-permission education; Back navigation lets users revise selections before persistence. Completion and choices are persisted locally in SecureStore. Goal framing, dietary preferences, and logging method synchronize best-effort through user preferences, while an explicitly accepted target synchronizes through the existing goals API. Its keyboard-aware scroll layout keeps target inputs and confirmation reachable on phone. Profile can revise all choices, and a kitchen-scale choice defaults Manual Search to grams. The target uses disclosed entered/default assumptions and is never saved when skipped. Dietary preferences are reference-only and do not filter foods or verify ingredients/allergens.
- FastAPI backend with SQLAlchemy models, Alembic migration for PostgreSQL, and SQLite bootstrap for phone preview.
- Premium material design-token package with semantic light/dark palettes, glass levels, elevation, macro/status colors, and motion presets. Expo permits the native interface to follow the system appearance, while a persisted `system`/`light`/`dark` preference drives the root shell, shared materials, Onboarding, Profile, Today, daily nutrient detail, Progress, Manual Search, Barcode, camera-permission education, nutrition-label capture, Saved Foods, Meal Builder, Custom Food, Recipe Library, Natural Entry, food provenance, saved-meal detail, and core Meal Confirmation surfaces; remaining feature-local styles are still being migrated.
- Core visual refresh for the in-app launch state, Today, camera capture, analysis review, manual search, and Progress using Expo Blur, atmospheric surfaces, structural loading rather than fake progress, staged analysis feedback, non-judgmental goal language, and safe intentional haptics: light feedback for shared actions, selection feedback for compact icon controls, medium feedback for camera capture, and success feedback for barcode matches.
- Read-only Premium preview from Profile that distinguishes included source-backed tools from planned product directions. It contains no checkout, price, trial, renewal, entitlement, or feature lock; a real subscription decision and implementation remain pending.
- USDA provider for food search and per-100g nutrient normalization.
- Open Food Facts provider for packaged-food barcode lookup.
- Provider search/detail lookups seed normalized cached food records; stale food-detail and exact barcode records claim a database-backed refresh lease and use jittered exponential backoff after a failure or no match while preserving a flagged cached snapshot, cached search matches can be returned when provider search fails, USDA/Open Food Facts requests use bounded retries for transient failures, and a transient provider HTTP failure permits the registry to try a configured fallback. When no live provider completes a lookup and no cached fallback exists, the API returns a safe request-correlated `503` rather than an unexpected server error.
- Manual food search with grams, ounces, or verified source-serving entry, keyboard-dismiss-on-drag behavior, and a sticky log action that stays above either the floating tabs or the keyboard after selecting a food. A server-confirmed food or saved-recipe log provides one success haptic and a user-controlled saved-state handoff to Today or another manual entry; an offline-queued log remains visibly distinct. With no search text, it shows the user's explicitly saved recipes tagged for the current meal period, labeled as saved recipes with review and one-tap log actions rather than inferred recommendations. Unverified household/volume servings are disabled instead of being assumed to equal 100g.
- Conservative natural entry supports up to six semicolon/newline-separated foods with explicit grams or ounces, or a simple count such as “two eggs.” It requires source-backed match selection for every item and permits counts only when the selected record has a verified gram serving. A server-confirmed multi-food meal gives one success haptic and a user-controlled saved-state handoff; offline queues remain visibly distinct. Cups and other volume measures are rejected instead of inferring mass.
- Barcode scanner route with barcode lookup, source/confidence display, grams/ounces/serving confirmation, persisted meal logging, nutrition-label extraction/manual-entry fallback, user custom barcode-product fallback, cached Open Food Facts repeat lookup, and serving/per-100g conflict warnings for packaged-food records. It uses success feedback only after a matched provider record or confirmed persisted meal, and a genuine save presents an explicit handoff to Today or another scan rather than redirecting immediately.
- Persistent meals, meal-item nutrition snapshots, daily diary endpoint, and home totals loaded from backend. Today links to a dedicated date-navigable daily diary that groups source-aware, confirmation-labeled meals by meal type and opens saved meal detail for review or adjustment. Both surfaces use structural loading, retryable error, and neutral empty-day states rather than false zero totals. The Home dashboard shows a compact factual seven-day diary-coverage cue without calling it a streak or nutrition grade, and lets users tap protein/carbohydrate/fat progress for a neutral explanation rooted in saved snapshots and adjustable daily targets. Its daily nutrient-detail route reuses the diary and goal data to show calories, macros, fiber, sugar, sodium, configured targets, and meal contributions; it never infers targets for a user. The timeline groups persisted meals by category and shows each meal's logged local time, accessible category-aware placeholder, calories, compact macro summary, food count, confirmation status, source, and expandable saved food/portion context. Each card keeps explicit Edit portions/Delete controls and adds a bounded swipe reveal that requires a second tap to edit or delete, preserving a non-destructive accessible baseline. Today also has an optional persisted hydration total with accessible 250/500 mL quick additions, exact milliliter adjustment, retry, and clear states; it has no target or medical claim. Profile adds one optional, device-local daily hydration reminder with a user-selected time, explicit permission handling, and direct cancellation.
- Recent foods are automatically persisted from logged meal items and shown in Manual Search.
- Favorite foods can be added/removed from food provenance, shown in Manual Search, searched/filtered/sorted in Saved Foods, removed individually, and bulk cleared for visible results. Each favorite can also carry up to ten account-private organization tags, which are searchable/filterable in Saved Foods and included in that user's export without changing provider source data or meal snapshots.
- Recent foods can be searched/filtered/sorted, removed individually, and bulk cleared for visible results in Saved Foods.
- Basic meal detail, source review, local date/time editing, provider-backed food replacement/addition, recoverable item removal, duplicate, split, explicit reordering, and meal deletion. The editor safely waits for a fetched meal to populate its local gram controls before calculating an adjusted total, and exposes a complete saved-snapshot nutrient breakdown for the portion entered, including fiber, sugar, and sodium when available. Users can search and select a provider record to replace a mistaken food identity or add a missing food at explicit grams before saving; the original food identity remains in a replacement snapshot context, while new items receive their own provider snapshot. A confirmed preparation label is stored without changing source nutrients. Confirmed added oil is stored separately from the food's per-100g basis and adds 9 kcal and 1 g fat per gram, preserving correct math when a food is edited or split. Removed items can be restored until save. Portion/date/time updates show a user-controlled completion state with success feedback; deletion requires in-app confirmation and produces a distinct removal state rather than using native alerts or abrupt redirects.
- Multi-food Meal Builder with source-backed search, inline gram adjustment, a persistent compact navigation-glass total above the tabs or keyboard with screen-reader nutrient summary, breakfast/lunch/dinner/snack/general-meal category selection, time-based category suggestions that preserve manual overrides, validated local meal-date/time entry and time presets, remove, duplicate, bounded drag-handle reordering with selection feedback, accessible reordering with persisted item order, and a bounded left-swipe Delete reveal that requires a second tap alongside the visible delete control. A genuine backend meal save uses one success haptic and a user-controlled saved-state handoff to Today or a new Meal Builder; offline-queued saves remain visibly distinct. It persists as either editable diary meals or reusable recipe templates.
- Recipe library with source-backed nutrition snapshots, category, ingredient-aware local search, meal-time/folder filtering, recent/use/name sorting, an account-private favorite flag, account-private tags, and one optional account-private folder. Users can add/remove favorites, create/rename/delete a folder, then move a loaded recipe to it or back to Unfiled; folder deletion requires an in-app confirmation and only removes those private links. The library also supports edit-through-Meal-Builder, log-to-today that preserves the saved category, usage tracking, in-context deletion confirmation, and recipe inclusion in basic current-user JSON export/account deletion. These controls do not modify recipe snapshots or provider data. A confirmed recipe log uses one success haptic and an explicit handoff to Today or back to the library rather than an abrupt redirect.
- Basic user nutrition goals and profile goal setup.
- Authenticated Manual Search, Barcode, Natural Entry, Meal Builder, fully reviewed Camera Confirmation, and Custom Food after its food record has saved can retain a confirmed source-backed meal snapshot in local Expo SQLite after an ambiguous network/server failure and explicitly sync it from Today using the original idempotency key. Camera images, new analysis requests, and uncreated custom-food records are never queued, and a queued meal is not presented as logged until sync succeeds.
- Basic current-user JSON data export endpoint and dedicated Data Controls export summary/native share action, including hydration entries. The app creates a temporary cache-only file for the system share sheet and removes it after the flow; it does not retain a local export file.
- Profile goal setup supports US and metric height/weight input modes, converts current height/weight entries when switching systems, and persists the selected unit system through user preferences.
- Basic weight tracking API and Profile logging/history workflow with entry editing/deletion, a unit-aware trend graph, selected-goal-direction feedback, and distinct entry, trend, and feedback layout regions on phone screens.
- Floating navigation now has a wide, slim, icon-only layout with evenly spaced Today, Progress, elevated Scan, Library, and Profile destinations plus a visible selected-icon capsule. The visual labels are omitted, while each tab retains an accessible name and selected state. Today is the logging hub, Library owns saved foods/provenance, and Progress is the range-based saved-snapshot insight route. A deliberate horizontal gesture on a top-level browse route switches Today, Progress, Library, and Profile; Scan remains tap-only to prevent accidental camera activation.
- Progress screen supports 7-day, 30-day, 90-day, and valid custom ranges up to 366 days, with a calorie line graph, saved-snapshot averages for calories/protein/fiber, a factual meal/protein/fiber logging-coverage summary, accessible selectable day detail, effective-date daily calorie targets, an optional descriptive saved-weight pattern, and navigable monthly rhythm from backend range/monthly insight endpoints. A separate daily nutrient-detail route handles saved totals and configured-target context for a selected day. Weight-integrated comparisons remain pending.
- Local register, login, refresh, logout, password-change, and session endpoints with hashed local passwords, signed short-lived JWT access tokens, rotated opaque refresh sessions, and SecureStore-backed mobile refresh/retry behavior. Password change verifies the current password, rejects reuse, revokes existing local sessions, and gives the requesting device a fresh session.
- Basic Profile session management for listing active local refresh sessions, showing privacy-preserving generic app labels, distinguishing the current device, and revoking another session.
- Custom-food backend endpoint and mobile create/edit/save/log/remove flow for user-entered per-100g records, including assistive nutrition-label extraction, safe per-100g normalization, local photo/raw-value comparison, and explicit review confirmation. Removal is owner-scoped, requires in-app confirmation, and preserves already logged meal snapshots. Nutrition-label capture uses the shared adaptive material system and retains manual entry before camera permission, after capture/import/extraction failures, and after a user captures a label; recoverable capture/import problems are inline rather than modal alerts.
- Food detail backend endpoint with serving options, provenance summary, and a bounded history of meaningful external provider-record changes.
- Mobile food detail/provenance route linked from manual search, barcode logging, camera meal confirmation, and saved meal detail.
- Food provenance UI showing provider/source reference, confidence, quality warnings including stale cached source and duplicate nutrition-conflict warnings, serving options, per-100g nutrients, original nutrient IDs, a bounded external source-history timeline, retained cross-provider conflict history with current-versus-historical labels, saved-meal snapshot fallback when live lookup fails, and source correction-report submission.
- Profile source-report history showing recent current-user correction reports, linked source metadata, safe review-status history, and source review links when available. Clerk-allowlisted staff can triage, resolve, or dismiss reports through a server-only operational API; internal notes and reviewer identity are never returned to the reporting user.
- Camera capture and image import, including an optional bounded three-view meal capture flow and optional 25 cm, 28 cm, or 30 cm known-plate visual cues when a whole round plate is visible. The cue is sent to analysis and preserved in confirmation/snapshots as context only; it cannot verify grams or increase portion confidence by itself. Capture/import errors remain inline with retry, import, and manual-entry recovery rather than interrupting the scan flow with native alerts; concurrent capture/import work is disabled while active.
- Camera analysis route that returns provider-backed nutrition estimates, reviewed meal-view count, conservative visible-portion ranges, visible-preparation cues, and possible hidden-ingredient review prompts.
- Camera confirmation screen with source links, cancellable in-flight analysis, direct retry after a transient analysis failure without losing the draft, explicit food confirmation, clearly labeled review-only scan cues, and provider-backed alternatives for uncertain scan labels. Alternate labels can be ordered by sanitized per-view visible support and the screen explains whether evidence is corroborated, conflicting, single-view, or unavailable. Corroboration does not increase confidence; a conflict lowers identity confidence and still requires explicit user selection. AI candidate-label search suggestions, in-card provider search replacement, and provider-backed sauce/topping add-ons with explicit grams remain available. Add-ons remain compact until requested, then expose their search, favorite/recent reuse, source, gram, removal, and accessible up/down ordering controls; selecting any record leaves grams blank, and the collapsed summary clearly blocks logging until every selected add-on has an entered amount. Common dressing, cheese, avocado, and butter shortcuts only prefill a provider search. Source review links remain available during management. The screen also includes inline source issue reporting, preparation selection, structured skin/bone/sauce/cheese/sugar review chips, direct numeric and keyboard-free stepper controls for portion and added oil/butter grams, notes, remove, duplicate, split, and mark-incorrect controls before logging. A genuine backend save clears the local photo draft, issues one non-blocking success haptic, and presents an explicit saved-state handoff to Today. Confirmed snapshots retain the scan-cue context without treating it as verified data.
- Request ID middleware, consistent error envelopes, structured logging, Docker Compose, CI skeleton, backend tests, and mobile typecheck. Mobile recovery copy explicitly distinguishes provider unavailability from a phone-to-server network failure while preserving an optional request reference.
- Configurable rolling-window limits for authentication and paid image-analysis routes, including `429` retry guidance and request IDs. Phone preview uses in-memory limits; production configuration requires atomic Redis shared limits, hashes client keys before storage, and fails closed with request-correlated `503` recovery copy if Redis is unavailable.
- Minimal database-backed audit events for local account/session lifecycle, password changes, export, and account deletion without credentials, tokens, meal data, images, or raw IP addresses. Data Controls can show the current account's safe event labels, outcomes, and timestamps without exposing internal request IDs or fingerprints.

### Partially Implemented

- **2026-07-21 image lifecycle update:** Camera photos remain temporary unless a user explicitly opts in during meal confirmation. A successful review holds normalized private inputs only through its bounded review window; explicit confirmation copies them to owner-scoped meal storage with the user's retention deadline, and unselected inputs are deleted. Saved Meal Detail can delete retained photos, the API authorizes expiring private-storage access only for the owning account, and account deletion cleans remaining assets before removing the profile. A configured S3-compatible store is required for actual signed URL viewing; cloud deployment and a mobile photo viewer remain pending.

- Authentication: Clerk provides the mobile email/password, email-verification, password-recovery, and Google OAuth flows; FastAPI verifies Clerk JWTs through JWKS and provisions the matching internal profile. A verified sign-up that still has Clerk-required fields is resumed for the same email instead of attempting an incompatible sign-in factor, and structured Clerk invalid-strategy errors clear the stale attempt before any advertised fallback. The prior local JWT/refresh-session implementation remains only for development compatibility and the bounded local-account migration path. Production rejects development/legacy auth, requires Clerk JWKS/issuer configuration, and requires Redis-backed protected-route limits. Hosted Clerk configuration, richer device/session management, trusted-proxy deployment validation, and Redis monitoring remain pending.
- Privacy controls: a versioned current-user JSON export/native-share flow with recipient acknowledgement and temporary cache-file cleanup, typed Living Nutrition profile deletion, private temporary-analysis-input cleanup, explicit completed-meal-photo retention, user-controlled retained-image deletion, owner-only expiring image access when private object storage supports it, owner-only security-activity visibility, restricted server-side audit review for configured Clerk subjects, minimal internal audit events, bounded database audit cleanup, and a signed audit-delivery outbox exist. Account deletion attempts every remaining owned private image before safely failing for a later retry. Production requires a deployment-managed `AUDIT_LOG_RETENTION_DAYS` value plus an HTTPS HMAC-signed audit receiver; local preview leaves delivery disabled and does not purge audit history. Managed-identity deletion coordination, deployed object-storage validation, production account lifecycle, audit-retention policy approval, and append-only/WORM receiver validation are still pending.
- Nutrition provenance: dedicated mobile source review, source correction reporting, current-user safe status history, Clerk-allowlisted server-only triage/resolution, and a bounded history of meaningful external provider-record changes exist; moderation policy/SLA, notification delivery, richer cache analytics, and richer provenance actions are pending.
- Data-quality checks: basic energy/macros checks, USDA/Open Food Facts required/parseable per-100g checks, negative nutrient flags, Open Food Facts serving-vs-per-100g and unverified-serving-basis flags, stale cached source warnings, bounded request-path and scheduled stale-provider refresh, meaningful external provider-record history, and persisted cross-provider duplicate nutrition-conflict evidence with current-versus-historical presentation exist. Each current food record also has a deterministic `complete`, `needs_review`, `insufficient_data`, or `user_entered` assessment with traceable signals; incomplete/invalid essential per-100g records cannot be logged from Manual Search, Barcode, or Camera Confirmation. Provider-specific quality tiers, duplicate ranking, and broader provider-cache quality analytics are pending.
- Camera workflow: single-photo capture/import and an optional bounded three-view capture with labeled previews; confirmation lets users switch between captured views before review. Each detected item now carries a conservative visible-portion range, optional visible-preparation cue, possible hidden-ingredient prompts, per-view visible-label support, and up to three provider-backed alternatives. Alternate suggestions are ordered by bounded visible support from the submitted views, while the UI describes corroborated, conflicting, single-view, or unavailable evidence. Candidate ordering is not a probability or automatic correction: corroboration never raises confidence, conflicting views lower identity confidence, and every item still requires user selection, preparation review, and grams. Source review, provider search replacement, provider-backed sauce/topping multi-adds with explicit grams, favorite/recent add-on reuse, a compact calorie/quality summary with intentional management controls, accessible add-on reordering, common category shortcuts that only prefill provider searches, add-on source review links, inline source issue reporting, gram adjustment, structured hidden-ingredient checks, oil adjustment, notes, split/duplicate/remove, and mark-incorrect controls exist. Reusable add-on groups and batch-selection refinements are pending; source reports now have a separate restricted staff review path.
- Favorites: basic add/remove, Manual Search display, and Saved Foods search/filter/sort/bulk-clear management exist; richer organization is pending.
- Recent foods: automatic persistence, Manual Search display, and Saved Foods search/filter/sort/removal/bulk-clear exist; richer organization is pending.
- Goals and weight tracking: Profile can save effective-date calorie/macro revisions with an optional maintain/cut/gain direction, view the read-only revision schedule, log/edit/delete weight entries, and show a unit-aware trend graph. Progress uses server-calculated selected-period comparisons with saved check-ins, date span, explicit insufficient/limited/observed states, and historical direction context when present. Pre-migration revisions without a stored direction remain unavailable rather than inferred. Longer-term aggregation is pending.
- Hydration: Today can read, create/update, and clear one user-owned daily milliliter total. It is explicitly optional and not a health target. Profile supports one opt-in local daily reminder with an editable local time; it is not a remote-notification or health-guidance system. Physical-device validation, history, trends, insights, and offline support remain pending.
- Insights: backend range, weekly, and monthly insight endpoints exist. The mobile Progress route uses the range endpoint for 7-day, 30-day, 90-day, and valid custom windows up to 366 days, including saved-snapshot calorie/protein/fiber averages, factual meal/protein/fiber logging coverage, effective-date daily calorie targets, selected-day detail, and a descriptive selected-period weight comparison. The comparison reports its saved check-ins, date span, data sufficiency, historical goal-direction context, and goal-revision count; it is not predictive or medical guidance. The monthly endpoint powers month navigation/rhythm. The daily nutrient-detail route is intentionally a separate read-only diary/goal presentation, not a new insight calculation.
- Custom foods: mobile create, edit, save, reuse, and log exists for user-entered per-100g records. A confirmed logged portion gives one success haptic and an explicit handoff to Today or a fresh custom-food entry, while an offline-queued portion remains visibly distinct. Barcode no-match can create a user custom packaged product for future barcode lookup. Assistive label extraction and explicit user review are implemented; stored evidence and richer verification are pending.
- Accessibility: a dedicated route reads and subscribes to reduced-motion, reduced-transparency, screen-reader, and text-scale status from the device, explains the app response, and offers an explicit refresh without pretending to change OS preferences. Shared screens now center content at a bounded 720px reading width on larger displays while retaining phone behavior. Reduced motion is respected by camera, macro-ring, and Today timeline entrance animation; shared glass materials and camera guidance use opaque fallbacks for reduced transparency; Today’s scan, search, quick-entry, Meal Builder, recipe, and barcode shortcuts announce their destination and review/confirmation expectation; Camera clearly labels capture-mode limitations and manual alternatives; Meal Confirmation exposes self-describing correction/replacement/provider-search/report controls with accessible expanded states and input hints; Saved Foods filter/sort, Profile unit/direction, onboarding logging/camera choices, Meal Builder categories, and Natural Entry source-record choices announce their selection state; Meal Builder time presets and destructive recipe/meal actions include recovery context; core actions have comfortable 44px touch targets; and Today loading regions are announced structurally rather than as nutrition values. Full VoiceOver/dynamic-type/contrast audit and remaining feature-screen dark-theme migration are pending.
- Onboarding: the introduction, goal framing, optional daily-target setup, dietary preferences, and logging-method preference are complete as a local-first first-run flow with best-effort preference/goal synchronization and Profile editing. The user must explicitly accept a visible general-wellness target before it is persisted or synced. Dietary preferences are intentionally non-filtering reference data, not allergen, ingredient, or medical validation.
- Offline behavior: the first durable queue supports authenticated, confirmed Manual Search, Barcode, Natural Entry, Meal Builder, Camera Confirmation, and already-saved Custom Food meal snapshots with an explicit Today sync action and account-deletion cleanup. Camera images/new analysis requests, custom-food creation, edits, background sync, conflict resolution, and broader offline drafts remain pending.
- Testing/documentation: initial mobile Jest coverage exists for the food provenance screen, saved-meal source fallback, food provenance presentation, saved-meal portion editing, in-app deletion confirmation, and completion states, camera capture/import recovery, camera confirmation save blocking, persisted-save success handoff, and offline-queue recovery, barcode no-match recovery, cooldown helpers, matched-save handoff, and dark-palette rendering, Food Detail, Manual Search, saved-meal detail, Recipe Library, Progress date-range controls, and daily nutrient detail, Home logging-hub rendering, custom-food manual label-review gating, confirmed-save handoff, and post-save offline-queue recovery, manual-search sticky layout, manual grams/ounces/servings logging controls, direct-food and saved-recipe backend-confirmed success handoffs, Saved Foods filtering/source actions, Meal Builder sticky-total layout, search/add/portion/save, backend-confirmed success handoff, and offline-queue recovery, Recipe Library structural loading, edit/log actions, and confirmed-save handoff, Natural Entry parsing/selection/logging, progress graph goal-status rendering, saved-food presentation, profile presentation, Profile US/metric unit switching, and floating-tab configuration; broader mobile flow tests plus deployment/testing/contribution guides need expansion.

## Missing By Phase

### Phase 1 Hardening

- OAuth or managed identity, account recovery, and richer device/session management beyond the implemented JWT/refresh/logout lifecycle and basic local session controls.
- Trusted-proxy policy, Redis deployment validation/monitoring, and broader production authorization/security review.
- Trusted-proxy policy, Redis deployment validation/monitoring, plus audit-retention policy approval and immutable delivery for sensitive operations. Restricted audit review and bounded database retention cleanup are implemented.
- EAS development, preview, and production profile validation. The profiles, development-client dependency, and light/dark native splash configuration are in place; owner account linkage, signing, and first cloud-build validation are pending.
- Environment-specific validation and clearer failure messaging for missing secrets.

### Phase 2 Accurate Manual Logging

- Admin correction-report management and richer food-source actions beyond the current basic source report form and Profile history.
- Richer favorite/recent organization beyond the current Saved Foods search/filter/sort/bulk-clear management screen and private favorite tags.
- More complete saved-meal composition editing beyond the implemented portions, date/time, provider-record replacement/addition, recoverable removal, duplicate, split, accessible persisted reordering, confirmed preparation labels, and confirmed added-oil math. Source-backed butter, sauces, and other add-ons in saved-meal detail, richer cooking adjustments, and broader unit support remain pending.
- Goal editor refinements beyond the current basic profile goal setup.
- More complete serving options and unit conversions.
- Richer natural-language parsing beyond the implemented grams/ounces and provider-backed simple-count syntax, including commercial-provider coverage and verified household-measure mappings.

### Phase 3 Packaged Foods

- Query-cache prefetching, provider-specific quality tiers/duplicate ranking, and distributed provider-outage validation beyond the current bounded retries, cross-provider HTTP fallback, barcode/detail cache, bounded query-cache expiry, database-backed request-safe and scheduled stale-provider refresh backoff, stale-source warning, deterministic record-quality assessment, durable duplicate-conflict evidence, and cached-search fallback.
- Richer Open Food Facts validation beyond the current serving-vs-per-100g conflict checks.
- Label-evidence support beyond the current user-entered barcode custom-food and assistive extraction/review flow.
- Consented nutrition-label evidence storage, retention/deletion controls, and richer verification beyond the current temporary local photo and confirmation workflow.
- Product correction report review and status management beyond the current source report submission and Profile history.

### Phase 4 Camera-Assisted Logging

- Durable async analysis jobs with polling, cross-device cancellation, and background-worker recovery beyond the current cancellable in-flight request.
- User validation of cross-image evidence wording, richer ambiguity fixtures, reusable add-on groups, and batch-selection refinements.
- Reusable add-on groups and batch-selection refinements beyond the current source-backed multi-add, favorite/recent reuse, explicit gram-entry controls, and source review.
- Multi-angle capture and known-reference-object support.
- Object storage abstraction and image-retention controls.

### Phase 5 Insights And Recipes

- Recipe organization beyond the implemented category-aware build/save/edit/log/delete workflow, private favorites/tags, and single-folder create/assign/unfile/rename/delete workflow; server-side search/pagination, continuous drag-and-drop reflow, and richer reuse controls remain pending. Recipes intentionally preserve category but not a fixed logging time.
- Longer-term goal-integrated weight insight beyond the implemented selected-period comparison, including user-approved aggregation or smoothing and migration handling for historical revisions with no stored direction.
- Goal editor.
- Hydration history, trends, reminders, insight integration, and offline support beyond the implemented optional Today total; plus the separate consistency module.
- Production account lifecycle, deployed object-storage validation, immutable audit delivery, and richer data export/download controls beyond the current Data Controls summary/native share flow and explicit scan-retention controls.

### Phase 6 Production Hardening

- Security review.
- Performance and accessibility audits.
- Offline drafts and sync queue.
- Provider cost controls and monitoring.
- Provisioning the implemented privacy-minimized Sentry hooks with managed DSNs, release artifacts/source maps, alert routing, and preview validation.
- App-store build and beta-test checklist.

## Next Recommended Slice

The next best implementation target is production-readiness hardening:

1. Add production account operations, recovery, and distributed abuse protection beyond the local JWT/refresh-session foundation.
2. Implement private meal-image storage, enforceable retention/deletion controls, and audit-log review/retention.
3. Expand provider cache refresh/resilience and admin correction-report management while continuing camera candidate-ranking improvements.

This keeps accuracy anchored in user-confirmed portions and provider nutrition records before we expand the more uncertain camera workflow.
